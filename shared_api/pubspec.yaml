name: shared_api
description: Package for everything which is shared between the different feature modules.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.2.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_lints: ^3.0.0
