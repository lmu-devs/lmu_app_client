name: core_routes
description: "Contains everything routing related to the app."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.2.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  go_router: ^15.1.0
  get_it: ^8.0.1

  equatable: ^2.0.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: any
  build_verify: any
  go_router_builder: ^2.9.0
