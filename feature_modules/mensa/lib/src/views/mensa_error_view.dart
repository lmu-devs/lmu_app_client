import 'package:flutter/material.dart';

class MensaErrorView extends StatelessWidget {
  const MensaErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("ERROR"),
    );
  }
}
