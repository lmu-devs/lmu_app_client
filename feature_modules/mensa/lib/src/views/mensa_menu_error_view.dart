import 'package:flutter/material.dart';

class MensaMenuErrorView extends StatelessWidget {
  const MensaMenuErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("ERROR"), //TODO: Add error message
    );
  }
}
