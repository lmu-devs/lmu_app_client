name: feature_module
description: A template for a feature module.

version: 0.1.0+1

environment:
  mason: ">=0.1.1"

vars:
  feature_name:
    type: string
    description: feature_name
    default: feature
    prompt: What is the name of the feature?
